# @prodaric/ui-engine

Prodaric Framework — Web Components & Form Engine (prodaric-button, prodaric-card, theme, form render).

## Instalación

```bash
npm install @prodaric/ui-engine
```

Requiere configuración de `@prodaric:registry=https://npm.pkg.github.com/` y autenticación en GitHub Packages.
